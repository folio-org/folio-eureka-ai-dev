# FOLIO Data Import App — Business Logic Context

> Generated by the `build-app-context` skill on 2026-06-16. Previous version sourced from docs.folio.org only.
> Sources: TestRail section(s) 338 + all child sections (905 cases, releases Umbrellaleaf/Trillium/Sunflower/Ramsons + older)
>          GitHub: folio-org/ui-data-import (README, CHANGELOG v8–v10, translations/en.json)
>                  folio-org/mod-data-import-converter-storage (ModuleDescriptor)
>          Jira: project UIDATIMP (80 issues: stories + bugs from v8–v10)
> Strings marked [confirmed] appear in both GitHub translations and TestRail cases.
> Strings marked [from source] come from GitHub only — verify in env before asserting.
> Strings marked [from cases] come from TestRail cases (weighted freq shown).
>
> **Release → UI version mapping:**
> Umbrellaleaf (R2 2026) = v10.1.x (in progress) | Trillium (R1 2026) = v10.0.0 | Sunflower (R1 2025) = v9.0.x | Ramsons (R2 2024) = v8.0.x

---

## What is Data Import

Data Import is the FOLIO app for batch importing records from external files (MARC 21, EDIFACT) into FOLIO. It creates and/or updates records in Inventory, MARC Authority, Orders, and Invoices apps. Two main areas: **Landing Page** (run imports, view logs) and **Settings** (configure profiles, file extensions, MARC field protections).

---

## Supported Record Types

### Can be Created AND Updated
- FOLIO Instances (Inventory)
- FOLIO Holdings (Inventory)
- FOLIO Items (Inventory)
- SRS MARC Bibliographic records
- MARC 21 Authority records

### Can be Created ONLY (not updated)
- FOLIO Orders and Purchase Order Lines
- FOLIO Invoices and Invoice Lines (EDIFACT)

### Cannot be Updated via Data Import
- SRS MARC Holdings records (create only)

---

## Profile Hierarchy (4 levels)

Recommended creation order: Field Mapping → Action → Match → Job

```
Job Profile  (top level — defines the full import workflow)
  ├── Match Profile  (optional — match incoming to existing records)
  │     ├── [if match found] → Action Profile → Field Mapping Profile
  │     └── [if no match] → Action Profile → Field Mapping Profile
  └── Action Profile  (without match — always applies)
        └── Field Mapping Profile
```

### Job Profile
- Entry point — user selects this when running an import
- Required fields: Name, Accepted data type (MARC or EDIFACT)
- Optional: Description, Tags
- Contains: linked Match profiles and/or Action profiles in an Overview accordion
- Match profiles can be **nested** to enable multi-field matching
- Action and Match profiles must exist before linking to a Job Profile
- Can be: edited, duplicated, deleted, tagged, exported/imported via API

### Match Profile
- Defines how incoming records are matched to existing FOLIO records
- Required fields: Name, incoming record format, existing record format, Match criteria (field + field)
- Since **Trillium** (UIDATIMP-1743): only **"Exactly matches"** is available as Match criteria — other criteria disabled
- Can match: Instance, Holdings, Item, MARC Bibliographic, MARC Authority
- Reusable across multiple Job Profiles
- Can be: edited, duplicated, deleted, exported/imported via API

### Action Profile
- Defines what action to take on the matched (or unmatched) record
- Required fields: Name, Action type, FOLIO record type
- Action types (exact UI text from source):
  - `Create (all record types except MARC Bibliographic, MARC Authority, or MARC Holdings)` [from source]
  - `Update (all record types except Orders, Invoices, or MARC Holdings)` [from source]
  - `Modify (MARC Bibliographic record type only)` [from source]
  - `Delete` — for MARC Authority only (new in **Trillium**, UIDATIMP-1730) *(verify label in env)*
- FOLIO record types available depend on Action type selected
- Since **Trillium** (UIDATIMP-1328): when linking a Field Mapping Profile, the list is filtered to show only profiles matching the selected FOLIO record type
- Links to one Field Mapping Profile (optional but common)
- An action profile can only link to **one** field mapping profile
- Edits to an Action Profile are reflected everywhere it is used
- "Linked data" record type option was **removed** in **Trillium** (UIDATIMP-1755) — do not assert its presence
- Can be: edited, duplicated, deleted, tagged, exported/imported via API

### Field Mapping Profile
- Maps MARC/EDIFACT fields to FOLIO record fields
- Two subtypes:
  - **MARC Field Mapping** — for Inventory, Authority records
  - **EDIFACT Field Mapping** — for Invoices
- Required fields: Name, Record type
- Can be: edited, duplicated, deleted, tagged, exported/imported via API

---

## Default and System Profiles

### System profiles — CANNOT be edited OR duplicated
- quickMARC - Create MARC holdings and inventory holdings field mapping profile
- quickMARC Derive - Create Inventory Holdings
- Default - Create MARC Authority action profile
- Default - Create MARC holdings action profile
- quickMARC - Create Holdings and SRS MARC Holdings job profile
- quickMARC - Derive a new SRS MARC Bib and Instance job profile

### System profiles — CANNOT be edited, but CAN be duplicated
- CREATE MARC Authority field mapping profile
- Inventory Single Record - Default Create/Update Instance field mapping & action profiles
- Inventory Single Record - Default Update MARC Bib field mapping & action profiles
- quickMARC Derive - Create Inventory Instance field mapping & action profiles
- Inventory Single Record - Default match profiles
- Inventory Single Record - Default Create/Update Instance job profiles

### System profiles — CAN be edited AND duplicated
- EDIFACT field mapping profiles
- Default - Create instance action profile
- Default - Create instance and SRS MARC Bib job profile
- Default - Create SRS MARC Authority job profile

### Critical rule
**If a system/default profile is edited, there is NO way to revert to the original defaults.** Always duplicate first and edit the copy.

---

## Import Flow

1. Staff uploads a MARC or EDIFACT file (browse or drag-and-drop)
2. File must have a registered extension (Settings → File Extensions) — if not, upload blocked
3. **Select job profile** window appears → staff select the appropriate profile
4. Profile details panel shows: Name, Accepted data type, Description, Tags, Overview (linked action/match profiles), previous jobs using this profile
5. Actions → Run → **Are you sure?** confirmation dialog → Run
6. Job appears in **Running** accordion with progress
7. Job completes → log row appears in Logs pane

---

## Landing Page Layout

### Left pane: Jobs
- Upload area (browse or drag-and-drop)
- **Running** accordion — currently running jobs with progress bar; can be collapsed
- Job profile selection window with search

### Central pane: Logs
Columns: File name, Status, Records, Job profile, Started running, Ended running, Run by, ID

- **Default view**: most recent logs for non-single-record imports
- Single Record Import logs hidden by default
- All columns sortable
- Actions → **View all** to see all logs including Single Record Imports
- Logs are **never deleted** unless a user with delete capability explicitly deletes them

---

## Log Statuses

| Status | Meaning |
|---|---|
| **Completed** | All records processed successfully |
| **Completed with errors** | Some records processed, some failed |
| **Fail** | No records processed |

### Viewing results (Completed / Completed with errors)
- Click **File name** in Logs → Summary view shows: Created, Updated, Discarded, Error counts per record type
- Clicking a record-level link opens the individual record in FOLIO

### Viewing errors (Fail / Completed with errors)
- Click the **row** (not file name) → per-record error log with error messages

---

## Log Filters (View All)

| Filter | Options |
|---|---|
| **Search** | Keyword (ID + File name), ID only, File name only |
| **Errors in import** | Yes / No |
| **Date** | From / To date range |
| **Job profile** | Select from dropdown |
| **User** | User look-up |
| **Inventory single record imports** | Yes / No |

---

## Detailed Log View

Clicking File name opens the detailed log with a record-level summary:

- **Summary table**: number of Created / Updated / Discarded / Error records per entity type (Instance, Holdings, Item, MARC Bib, Authority, Order, Invoice)
- Clicking the count link → list of individual records with their outcome
- For errors: clicking the title link shows the system error message

---

## File Extensions (Settings)

Located in **Settings → Data Import → File Extensions**

- Controls which file formats are accepted for upload
- If a file has an extension NOT in the list → upload blocked before profile selection
- Each extension: extension value (e.g. `.mrc`, `.dat`), data type (MARC, EDIFACT), blocked/allowed status
- Can be: created, edited, deleted

---

## MARC Field Protections (Settings)

Located in **Settings → Data Import → MARC Field Protection**

- Fields listed here are NOT overwritten during record updates even if incoming record has different data
- Can protect an entire field or a specific field + subfield combination
- **System defaults (cannot be removed)**:
  - `001` — FOLIO Instance HRID
  - `999 ff` — SRS record UUID (`$s`) and FOLIO Instance UUID (`$i`)
- User-defined protections: can be added, edited, deleted
- **If a system default protection is edited, it cannot be reverted** — always duplicate/recreate
- Since **Trillium** (UIDATIMP-1699): users can **override** default field protections specifically within MARC Authority Field Mapping Profiles

---

## Single Record Import (from Inventory)

A special import path triggered from Inventory app (not from Data Import landing page):
- **Actions → Overlay source bibliographic record** — updates existing MARC bib from external Z39.50/SRU source
- **Actions → Find and save** (or Import) — imports a new record from an external source

Single Record Import behavior:
- Logs appear only in **View All** with "Inventory single record imports = Yes" filter
- Not shown in the default Logs pane view
- Uses dedicated system profiles that cannot be edited:
  - Inventory Single Record - Default Create Instance job profile
  - Inventory Single Record - Default Update Instance job profile
  - Inventory Single Record - Default match for existing SRS record
  - Inventory Single Record - Default match for no SRS record

---

## Instance Source Behavior on Import

| Scenario | Result |
|---|---|
| New MARC bib imported | Instance created with source = MARC; SRS record created |
| Existing instance updated via Data Import | SRS MARC record updated; Instance updated from SRS |
| Instance created without MARC (FOLIO native) | source = FOLIO; no SRS record |
| LDR 05 = 'd' in imported MARC | Instance set for deletion; Suppress from discovery + Staff suppress = Yes automatically |

---

## Required Capability Sets (Eureka)

### Data Import app
| Action | Capability Set | Legacy Okapi Permission |
|---|---|---|
| Upload files, run import, view logs | `Data - UI-Data-Import - Manage` | `Data import: Can upload files, import, and view logs` |
| View logs only | `Data - UI-Data-Import - View` | `Data import: Can view logs` |
| Delete import logs | `Data - UI-Data-Import Logs - Delete` | `Data import: Can delete import logs` |
| Delete MARC Authority records | `Data - UI-Data-Import - Delete` *(Trillium, UIDATIMP-1731 — verify exact name in env)* | n/a |

### Data Import Settings
| Action | Capability Set | Legacy Okapi Permission |
|---|---|---|
| View settings only | `Settings - UI-Data-Import Settings ReadOnly - View` | — |
| View, create, edit, delete settings | `Settings - UI-Data-Import Settings - Manage` | `Settings (Data import): Can view, create, edit, and remove` |

### Additional capabilities commonly required
| Scope | Capability Set |
|---|---|
| Inventory (view/manage records) | `Data - UI-Inventory All-Permissions Temporary - Manage` |
| quickMARC editor | `Data - UI-Quick-Marc Quick-Marc-Editor - Manage` |
| Data Export (view exported records) | `Data - UI-Data-Export - Edit` |
| ECS — update shared/shadow records on Central tenant | `Consortia Data-Import Central-Record-Update` |

---

## Exact UI Texts

> Use these verbatim in expected results. Never paraphrase.

### Toast / Callout Messages

| Event | Exact text | Source |
|---|---|---|
| Job profile created/updated/deleted | `The job profile "{name}" was successfully {action}` | [confirmed] |
| Match profile created/updated/deleted | `The match profile "{name}" was successfully {action}` | [confirmed] |
| Action profile created/updated/deleted | `The action profile "{name}" was successfully {action}` | [confirmed] |
| Field mapping profile created/updated/deleted | `The field mapping profile "{name}" was successfully {action}` | [confirmed] |
| Import logs deleted | `{logsNumber} data import logs have been successfully deleted.` | [from source] |
| MARC field protection deleted | `The MARC field protection {term} was successfully deleted` | [from source] |
| Upload error | `There was a problem uploading the file. Please try again` | [from source] |
| Upload definition conflict | `Upload definition was deleted by another user. Please try again.` | [from source] |
| File deletion error | `Error while deleting {name} file` | [from source] |
| Generic record created | `New record created:` | [from source] |
| Generic record updated | `Record updated:` | [from source] |
| Generic record deleted | `Record deleted:` | [from source] |

> **`{action}` placeholder** resolves to `created`, `updated`, or `deleted`.

### Modal / Dialog Titles

| Modal title | Trigger | Source |
|---|---|---|
| `New field mapping profile` | Click "New" in Field mapping profiles list | [confirmed — 80x cases] |
| `New action profile` | Click "New" in Action profiles list | [confirmed — 80x cases] |
| `New job profile` | Click "New" in Job profiles list | [confirmed — 78x cases] |
| `New match profile` | Click "New" in Match profiles list | [confirmed — 78x cases] |
| `Cancel import job?` | Actions > Cancel running job | [from source] |
| `Cancel multipart import job?` | Cancel a split-file import | [from source] |
| `Import job finished before cancellation` | Job completes before cancel request is processed | [from source] |
| `Delete data import logs?` | Select logs and click Delete | [from source] |
| `Import blocked` | Upload file with unregistered extension | [from source] |
| `Inconsistent file extensions` | Upload multiple files with different extensions | [from source] |
| `Not enough space` | Insufficient server disk space for upload | [from source] |
| `Reset all file extension mappings` | Restore defaults in File Extensions settings | [from source] |
| `Are you sure you want to run this job?` | Click Actions > Run after selecting job profile | [from cases — 2x] |
| `Select affiliation` | ECS tenant context switch | [from cases — 8x] |
| `Select Match Profiles` | Add match profile to job profile overview | [from cases — 4x] |
| `Confirm removal` | Unlink a profile from a job profile | [from source] |

### Button Labels (key actions)

`Actions`, `Save as profile & Close`, `Save as profile & close`, `Open`, `Run`, `Search`, `or choose files`, `or select files`, `Save & close`, `Close`, `Link Profile`, `Edit`, `Delete`, `New match profile`, `Import`, `Next`, `View source`, `No, do not cancel import`, `Yes, cancel import job`, `Yes, delete`, `No, do not delete`, `Reset all`, `Choose other files to upload`

### Error / Warning Messages

| Condition | Message text | Source |
|---|---|---|
| File extension not registered | `You cannot upload files with this file extension` | [from source] |
| Multiple files with mixed extensions | `You cannot upload files with {highlightedText}. Please upload files with the same extension` | [from source] |
| File with no extension | `You cannot upload a file without a file extension` | [from source] |
| Syntax error in field mapping | `Please correct the syntax to continue` | [from source] |
| Non-MARC value missing quotes | `Non-MARC value must use quotation marks` | [from source] |
| Action not selected before save | `Action must be selected before the profile can be saved` | [from source] |
| Duplicate file extension | `File extension {name} already exists` | [from source] |
| Invalid file extension format | `File extension {name} is not a valid format` | [from source] |
| Delete protected MARC field | `This MARC field protection cannot be deleted, as it is in use by one or more records.` | [from source] |
| Request failed | `Request failed. Try again` | [from source] |

### Upload Area Texts

| Label | Text | Source |
|---|---|---|
| Upload prompt (idle) | `Drag and drop to start a new data import job` | [from source] |
| Upload prompt (active drag) | `Drop to start import` | [from source] |
| Empty running jobs | `No running jobs to show` | [from source] |

---

## Common Verification Patterns

### 1. Profile Created → Detail View Shown

```
Action:   Click "Save as profile & Close" in the New [profile type] form
Expected: The [profile type] was successfully created.
          Detail view of the created profile is shown in the 3rd or 4th pane.
          Profile name appears in the profile list.
```

### 2. Import Job Completes → Log Entry Appears

```
Action:   Upload file, select job profile, click Actions > Run > confirm
Expected: Job appears in "Running" accordion with a progress bar.
          After completion, log entry appears in Logs pane with status "Completed"
          (or "Completed with errors").
          Log row shows: File name, Status, Records, Job profile, Started running,
          Ended running, Run by, ID.
```

### 3. Import Blocked by Unregistered Extension

```
Action:   Upload a file whose extension is not in Settings > Data import > File extensions
Expected: "Import blocked" modal appears:
          "You cannot upload files with this file extension"
          File is not uploaded; no import job is created.
```

### 4. Verify Match Profile Record Type Options (ECS)

```
Action:   On Member tenant > Settings > Data import > Match profiles > New
Expected: "New match profile" page opens.
          "Details" accordion shows: Instance, Holdings, Item, MARC Bibliographic,
          MARC Authority.
          Orders and Invoices NOT available on Member tenant.
```

### 5. Field Mapping Profile Filtered by FOLIO Record Type (Trillium+)

```
Action:   In Action Profile, select "Holdings" in "FOLIO record type*" field,
          click "Link Profile"
Expected: Only Field mapping profiles with FOLIO record type "Holdings" are displayed.
          Profiles of other record types are not shown.
```

---

## ECS / Multi-Tenant Notes

**47 ECS-enabled test cases** found in this section (sections: Consortia subsections under MARC Bib, MARC Authority, MARC Holdings).

### Tenant Setup
Preconditions use: **Central**, **Member 1**, **Member 2** tenants.

### ECS-Specific Business Rules
1. **Central tenant** can create Action/Field Mapping Profiles for all FOLIO record types including Order and Invoice.
2. **Member tenants** can only create profiles for: Instance, Holdings, Item, MARC Bibliographic, MARC Authority — Order and Invoice options are **not shown**.
3. **"Default - Create Holdings and SRS MARC Holdings" job profile is hidden on Central tenant** (Trillium, UIDATIMP-1733).
4. **Sharing deleted instances** on Central should not produce errors (Ramsons test case: `No error appeared after sharing deleted instance`).
5. **Updating shared records from Central** requires `Consortia Data-Import Central-Record-Update` capability; without it the update is blocked (Trillium test cases).
6. **Affiliation switching** modal title: `Select affiliation`.

---

## Key Business Rules for Test Cases

> Source tags: (cases) = ≥3 case titles, (jira) = acceptance criterion/bug fix, (github) = source/changelog, (cases+jira) = confirmed in both.

1. **4-level profile hierarchy must be built bottom-up** — Field Mapping → Action → Match → Job; each level must exist before the next can reference it. (cases)
2. **Match profile is optional** — if absent: all incoming records treated as new (Create action); no matching attempted. (cases)
3. **Action profile links to exactly one field mapping profile** — cannot link multiple; changing the mapping requires editing or duplicating the action profile. (cases)
4. **Field mapping profiles filtered by FOLIO record type in the link dialog** *(Trillium, UIDATIMP-1328)* — only profiles of matching type appear when linking to an Action Profile. (jira + cases)
5. **Only "Exactly matches" is available as Match criteria** *(Trillium, UIDATIMP-1743)* — other criteria are disabled/hidden. (jira)
6. **"Linked data" removed from FOLIO record type selector** *(Trillium, UIDATIMP-1755)* — do not assert its presence. (jira)
7. **Editing an Action Profile propagates everywhere it is used** — changes affect all Job Profiles containing it. (cases)
8. **System defaults cannot be reverted after editing** — always duplicate first; "cannot edit" profiles are fully protected; "can edit" profiles are editable but irreversible. (cases)
9. **File must have a registered extension** — upload blocked at file selection if extension not in Settings → File Extensions; "Import blocked" modal shown before profile selection. (cases + github)
10. **Single Record Import logs hidden by default** — only visible via Actions → View all + "Inventory single record imports = Yes" filter. (cases)
11. **001 and 999 ff fields are always protected** — cannot be overwritten on any update import; system-level protection, not removable. (cases)
12. **EDIFACT invoices/orders: Create only** — no Update action available for Orders or Invoices. (cases + github)
13. **MARC Holdings: Create only** — Data Import cannot Update existing MARC Holdings records. (cases)
14. **Completed with errors = partial success** — some records imported; error log shows which records failed and why. (cases)
15. **Fail status = no records processed** — clicking row shows system error; no record changes occurred. (cases)
16. **Logs are never auto-deleted** — only a user with `Data - UI-Data-Import Logs - Delete` capability can delete them. (cases)
17. **Delete logs is a separate capability** — not included in the Manage capability; must be explicitly assigned. (cases)
18. **Running accordion shows live progress** — can be collapsed but jobs continue running; important for preconditions requiring waiting for completion. (cases)
19. **Nested match profiles for multi-field matching** — Job Profile can have Match Profiles inside Match Profiles; if outer match fails → falls through to inner submatch. (cases)
20. **LDR 05 = 'd' triggers Set for Deletion** — imported records with this Leader value automatically have "Suppress from discovery" and "Staff suppress" set to Yes. (cases + jira)
21. **"Set for deletion" field added to Instance Field Mapping profile** *(Trillium, UIDATIMP-1742)* — field `Admin data: Set for deletion` is now available as a mapping option. (jira)
22. **Delete action for MARC Authority records** *(Trillium, UIDATIMP-1730)* — a new "Delete" action type is available for MARC Authority Action Profiles; requires dedicated `Data - UI-Data-Import - Delete` capability (UIDATIMP-1731). (jira)
23. **Cancelled jobs cannot be restarted** — after cancellation, job status = "Stopped by user"; no resume option. (github)
24. **Bulk Edit job profiles excluded from Data Import log** *(Ramsons, UIDATIMP-1665)* — bulk edit entries no longer appear in the DI log. (jira)
25. **MARC Authority field mapping can override default field protections** *(Trillium, UIDATIMP-1699)* — users can mark specific protected fields as overridable in the MARC Authority field mapping profile. (jira)
26. **ECS: Member tenants cannot create Order/Invoice profiles** — FOLIO record type options on member tenants exclude Order and Invoice. (cases)
27. **ECS: Updating shared records from Central requires dedicated capability** — `Consortia Data-Import Central-Record-Update` must be assigned; without it, shared/shadow record updates are blocked. (cases)
28. **Profile export/import is API-only** — no UI button; done via FOLIO API; relevant for preconditions involving profile migration between environments. (cases)
29. **Import logs columns are all sortable** — File name, Status, Records, Job profile, Started running, Ended running, Run by, ID. (cases)
30. **Donor information section in Order field mapping** *(Ramsons, UIDATIMP-1656)* — uses "Add donor" button / "Donor information" accordion. (jira + cases)

---

## Known Gaps / Items to Verify

> Strings or rules that could not be confirmed from two independent sources. Verify in env before relying on them.

- [ ] **Exact toast text on profile creation** — source has `New record created:` but TestRail cases show `"A profile was created"` (w=2.4) — which is current in env?
- [ ] **`Data - UI-Data-Import - Delete` capability exact name** — UIDATIMP-1731 introduced it in Trillium; not yet confirmed in TestRail cases.
- [ ] **`Admin data: Set for deletion` exact field label** — UIDATIMP-1742, not yet in cases; confirm label in Trillium/Umbrellaleaf env.
- [ ] **MARC Bibliographic Updates mapping profile new sub-actions** — `Replace`, `Find and remove`, `Delete`, `Add (if not duplicate)`, `Add` (UIDATIMP-1757–1761); no fixVersion yet, likely Umbrellaleaf — verify availability.
- [ ] **`Consortia Data-Import Central-Record-Update` full Eureka format** — appears 5× in cases as abbreviation; verify full `type/table - resource - action` format in env.
- [ ] **Cancel job entry point** — verify whether "Cancel running job" appears as an option in the Actions menu or as a standalone button on the running job row.
