# FOLIO eHoldings App — Business Logic Context

> Generated by the `build-app-context` skill on 2026-06-16.
> Sources: TestRail section 113 (166 cases, releases Umbrellaleaf–Ramsons)
>          GitHub: folio-org/ui-eholdings (README, en.json, CHANGELOG)
>          Jira: project UIEH (100 issues retrieved)
> Strings marked [confirmed] appear in both GitHub translations and TestRail cases.
> Strings marked [from source] come from GitHub translations only — verify in env before asserting.
> Strings marked [from cases] come from TestRail cases (weighted freq shown).
> Previous version header: manual documentation (docs.folio.org only).

---

## What is eHoldings

The eHoldings app manages the library's electronic resource holdings via an external knowledge base (KB). It connects to EBSCO's HoldingsIQ API — currently the **only supported external KB**. Changes made in FOLIO eHoldings or EBSCO Holdings Management are **immediately reflected** in both interfaces (real-time sync via API).

eHoldings allows libraries to:
- Browse and search providers, packages, and titles from the EBSCO KB
- Select/deselect packages and titles for the library's holdings
- Edit coverage settings, proxy settings, and custom labels
- Create custom packages and titles
- Link e-resources to Agreements in the Agreements app
- View usage & analysis (cost-per-use) data exported via Export Manager

---

## Key Terms

| Term | Definition |
|---|---|
| Provider | Content vendor (e.g. Elsevier, Wiley) — sourced from EBSCO KB; not editable |
| Package | Collection of titles from a provider; can be KB or custom |
| Title | Individual e-resource (journal, e-book, etc.) |
| Resource | A title in the context of a specific package (title + package combination) |
| KB | Knowledge base — EBSCO HoldingsIQ; the only supported external KB |
| Custom package | Library-created package (not from KB); can be deleted |
| Custom title | Library-created title linked to a custom package; can be deleted |
| Holdings status | Whether the library has selected (added) a package or title |
| Access status type | Library-defined label for a title's local access status |
| Usage Consolidation | SUSHI-based cost-per-use analytics feature requiring separate configuration |

---

## Record Hierarchy / Architecture

```
Provider (content vendor, e.g. Elsevier, Springer)
  └── Package (collection of titles, e.g. "Elsevier Freedom Collection")
        └── Title (individual e-resource, e.g. a journal or e-book)
              └── Resource (title in context of a specific package = title + package combination)
```

All Provider, Package, and Title KB data lives in EBSCO's knowledge base — FOLIO accesses it via HoldingsIQ API. Only library-local data (selection state, coverage, proxy overrides, custom labels, tags, notes) is stored in FOLIO.

---

## Lifecycle / Workflow

### Package / Title Holdings Status

| Status | Description |
|---|---|
| **Not Selected** | Exists in KB but not in library's holdings (default for KB content) |
| **Selected** | Library has added this package/title to their holdings |
| **{ N } of { M } titles selected** | Package is partially selected (some titles added) |

Status transitions require `Procedural - UI-Eholdings Package-Title Select-Unselect - Execute`.

### Package Visibility (Show to Patrons)

| Setting | Description |
|---|---|
| **Show to patrons** | Package/title is visible in discovery |
| **Not shown to patrons** | Hidden from patron-facing discovery interfaces |

---

## Main Sections / UI Structure

### Search Tabs
- **Providers** tab
- **Packages** tab
- **Titles** tab

Providers tab is highlighted by default when opening eHoldings app. [confirmed]

### Panes
- `Search & filter` pane — left pane with search box and filters [confirmed]
- Provider / Package / Title / Resource detail record pane — right pane [confirmed]
- `Export settings` modal — triggered from Actions → Export [confirmed]

### Accordions (on record detail views)

**Package detail record:**
- `Package information` [from source]
- `Package settings` [confirmed]
- `Coverage settings` [confirmed]
- `Titles` (list of titles in package) [confirmed]
- `Agreements` [confirmed]
- `Notes` [confirmed]
- `Tags` [confirmed]
- `Usage & analysis` (requires Usage Consolidation configured) [confirmed]

**Title+Package (Resource) detail record:**
- `Resource information` [confirmed]
- `Coverage settings` [confirmed]
- `Custom labels` [confirmed]
- `Agreements` [confirmed]
- `Notes` [confirmed]
- `Tags` [confirmed]
- `Usage & analysis` [confirmed]

**Title detail record:**
- `Title information` [confirmed]
- `Packages` (list of packages this title appears in) [confirmed]
- `Notes` [confirmed]
- `Tags` [confirmed]

**Provider detail record:**
- `Packages` accordion [confirmed]
- `Notes` [confirmed]
- `Tags` [confirmed]

### Key Navigation Paths
- `Settings >> eHoldings` (KB credentials, root proxy, custom labels, usage consolidation, assigned users, access status types)
- `Settings >> eHoldings >> Assigned users`
- `Settings >> eHoldings >> Access status types`
- `Settings > eholdings > Custom labels`
- `Actions >> Edit` (to enter edit mode on any record)
- `Actions >> Export package (CSV)` (on package records)
- `Actions >> Export title package (CSV)` (on resource records)

---

## Exact UI Texts

> Use these verbatim in expected results. Never paraphrase.

### Toast Messages

> ✅ Re-verified against `folio-org/ui-eholdings/translations/ui-eholdings/en_US.json` (2026-07-21): every row below matches, including all previously `[from source]` settings/export toasts — treat them as confirmed. The one exception is the last row (title-selection lag warning), which is only in an open Jira story, not the translation file.

| Event | Toast text | Source |
|---|---|---|
| Custom package created | `Custom package created.` | [confirmed] |
| Custom title created | `Custom title created.` | [confirmed] |
| Custom package name already exists | `Custom Package with the provided name already exists` | [confirmed — C1404904] |
| Custom title name already exists | `Custom Title with the provided name already exists` | [confirmed — C1404905] |
| Provider record saved | `Updates saved. Note: A proxy update may take a few minutes and may require refreshing page.` | [confirmed] |
| Package record saved | `Package saved. Note: A proxy or token update may take a few minutes and may require refreshing page.` | [confirmed] |
| Resource (Title+Package) saved | `Title was updated.` | [confirmed] |
| Title saved | `Title saved.` | [confirmed] |
| Title removed from custom package | `Title removed from package.` | [confirmed] |
| KB settings updated | `KB settings updated` | [from source] |
| KB credentials saved | `{name} has been saved` | [from source] |
| KB credentials deleted | `{kbName} has been removed.` | [from source] |
| Root proxy updated | `Root proxy updated` | [from source] |
| Custom labels updated | `Custom labels have been updated` | [from source] |
| Usage Consolidation settings saved | `Usage Consolidation settings are updated.` | [from source] |
| Access status type deleted | `The access status type {name} has been deleted.` | [from source] |
| UC titles exported | `Titles have been successfully exported.` | [from source] |
| Package/Resource export triggered | `Your eholdings export (Job ID: {jobName}) is in progress and will be available on the Export manager app. The export may take several minutes to complete.` | [from source] |
| Export request failed | `The export request has failed. Please try again.` | [from source] |
| Title selection status lag warning | `Updates to title selection status in the Titles accordion may take several seconds to display.` | [from source, Jira UIEH-open] |

### Modal / Dialog Titles

| Modal | Trigger | Source |
|---|---|---|
| `Remove package from holdings?` | Deselect KB package | [confirmed] |
| `Delete custom package` | Delete a custom package | [confirmed] |
| `Remove title from holdings?` | Remove title from holdings | [confirmed] |
| `Remove resource from holdings?` | Remove resource (title+package) | [confirmed] |
| `Delete agreement line` | Delete link in Agreements accordion | [confirmed] |
| `Confirm navigation` | Navigating away with unsaved changes | [confirmed] |
| `Export settings` | Actions → Export package (CSV) or Export title package (CSV) | [confirmed] |
| `Filter packages` | Apply Packages facet on Titles search | [from cases (x3)] |
| `Add all titles in a package to holdings` | Confirmation when selecting entire package | [from source] |
| `Already assigned to a knowledge base` | Trying to assign user already assigned to another KB | [from cases (x1)] |

### Modal Body Messages

| Modal | Body text | Source |
|---|---|---|
| Remove package | `Are you sure you want to remove this package and all its titles from your holdings? All customizations will be lost.` | [from source] |
| Delete custom package | `Are you sure you want to delete this package? By deleting this package it will no longer be available for selection and all customization will be lost. If a title only appears in this package it will no longer be available for selection.` (full sentence — don't truncate after "lost.") | [confirmed — C605986] |
| Confirm navigation | `Your changes have not been saved. Are you sure you want to leave this page?` | [confirmed] |
| Delete agreement line | `Are you sure you want to delete this agreement line: {recordName}?` | [from source] |

### Button Labels (key actions)

**General / Record actions:**
`Edit`, `Actions`, `Cancel editing`, `Save & close`, `Cancel`, `Delete`

**Holdings actions:**
`Add to holdings`, `Add package to holdings` (Actions menu option + button on package record; opens the "Add package to holdings" modal), `Remove from holdings`, `Remove title from holdings`, `Add all titles in a package to holdings`, `Add package (all titles) to holdings` (button inside that modal). [confirmed — C688]

**Navigation modal buttons:**
`Continue without saving`, `Keep editing`

**Package/resource removal confirmation:**
`Yes, remove`, `No, do not remove`, `Yes, delete`, `No, do not delete`

**Export:**
`Export package (CSV)`, `Export title package (CSV)`, `Export`, `Export titles`, `View titles`

**Agreements accordion:**
`New` (opens new agreement tab), `Add` (links existing agreement), `Delete agreement line`, `Unassign`

**Settings:**
`New` (new KB), `Edit knowledge base credentials`, `New knowledge base credentials`, `Delete`, `Select user`, `Assign users`, `Unassign`

**Coverage:**
`Add date range`

**Custom packages / titles:**
`New custom package`, `New custom title`

### Error / Warning Messages

| Condition | Message text | Source |
|---|---|---|
| UC summary unavailable | `Unable to provide summary information.` | [from source] |
| UC title list fails | `Unable to retrieve title list. Please try again` | [from source] |
| UC export service error | `Export unavailable due to an service error. Please try again.` | [from source] |
| UC export timeout | `Export times out. Please retry.` | [from source] |
| Export exceeds 200k limit | `Exceeds 200,000 export title limit` | [from source] |
| Root proxy warning | `Warning: Changing the root proxy setting will override the proxy for all links and resources currently set to inherit the root proxy selection.` | [from source] |
| Assigned users error | `Unable to complete this operation. Please try again.` | [from source] |
| Invalid proxy selected | `Invalid proxy selected. Please contact EBSCO support.` | [from source] |
| Customer ID blank | `Customer ID cannot be blank` | [from source] |
| API key blank | `API key cannot be blank` | [from source] |
| Invalid UC ID | `Invalid Usage Consolidation ID. Please enter a valid ID.` | [from source] |
| UC credentials invalid | `Usage Consolidation credentials are not valid.` | [from source] |
| Coverage statement too long | `Statement must be 350 characters or less.` | [from source] |
| Coverage statement blank | `Statement field cannot be blank if selected.` | [from source] |
| Custom label invalid char | `Invalid character entered. Please revise the custom label.` | [from source] |
| Package requires a package link | `Package *` (field required indicator on Title creation) | [from source] |

---

## Status Values

### Holdings / Selection Status
`Selected`, `Not selected`

### Package visibility (Show to Patrons)
`Show to patrons`, `Not shown to patrons` [from source]

### Package Content Type
Aggregated Full Text, Abstract and Index, E-Book, E-Journal, Mixed Content, Online Reference, Print, Streaming Media, Unknown

### Publication Type (Title)
Audiobook, Book, Book Series, Database, Journal, Newsletter, Newspaper, Proceedings, Report, Streaming Audio, Streaming Video, Thesis & Dissertation, Website, Unspecified

### Title visibility (Hidden column, Trillium+)
`Hidden` — shown in the Titles accordion status column for titles hidden in HLM [from source, UIEH-1464]

---

## Capability Sets (Eureka)

> Extracted from TestRail preconditions and cross-referenced with eholdings.md Eureka mapping.
> Legacy permission names shown in parentheses — do not use in new test cases.

| Action | Capability Set |
|---|---|
| View providers, packages, titles (app access) | *(included with app access)* |
| Edit providers, packages, titles | `Data - UI-Eholdings Records - Edit` |
| Select / unselect packages and titles | `Procedural - UI-Eholdings Package-Title Select-Unselect - Execute` |
| Create / delete custom packages and titles | `Data - UI-Eholdings Titles-Packages Create-Delete - Manage` |
| View usage & analysis (cost-per-use) | `Data - UI-Eholdings Costperuse - View` |
| Manage Usage Consolidation settings | `Settings - UI-Eholdings Settings Usage-Consolidation Create-Edit` |
| View Usage Consolidation settings | `Settings - UI-Eholdings Settings Usage-Consolidation` |
| Assign / unassign user from KB | `Settings - UI-Eholdings Settings AssignedUser - Edit` |
| View / manage Access status types | `Settings - UI-Eholdings Settings Access-Types - View` / `...Access-Types - Manage` |
| View / manage Custom labels | `Settings - UI-Eholdings Settings Custom-Labels - View` |
| Edit Root proxy | `Settings - UI-Eholdings Settings Root-Proxy - Edit` |
| Export packages / titles | `Data - UI-Export-Manager Export-Manager - Manage` |
| View agreements (for linking) | `Data - UI-Agreements Agreements - View` |

> ⚠️ Legacy permissions found in older cases (do not use in new cases):
> - `eHoldings: Can view providers, packages, titles detail records`
> - `eHoldings: Can edit providers, packages, titles detail records`
> - `eHoldings: Can create and delete custom packages and titles`
> - `eHoldings: Can view Usage & analysis data`

---

## Common Verification Patterns

> Copy these into expected results. They reflect how the team actually writes assertions for this area.

### 1. Package saved (proxy/token update)

```
Action:   Click "Save & close" on edited Package record
Expected: Package record detail view is displayed.
          Toast: "Package saved. Note: A proxy or token update may take a few minutes and may require refreshing page."
```

### 2. Provider saved (proxy update)

```
Action:   Click "Save & close" on edited Provider record
Expected: Provider record detail view is displayed.
          Toast: "Updates saved. Note: A proxy update may take a few minutes and may require refreshing page."
```

### 3. Confirm navigation on unsaved changes

```
Action:   Click "X" icon or "Cancel" while editing a record with unsaved changes
Expected: "Confirm navigation" modal appears with message:
          "Your changes have not been saved. Are you sure you want to leave this page?"
          Buttons: "Keep editing" | "Continue without saving"
```

### 4. Export package triggered

```
Action:   Actions → Export package (CSV) → choose fields → click "Export"
Expected: "Export settings" modal closes.
          Toast: "Your eholdings export (Job ID: {jobName}) is in progress and will be available on the Export manager app."
          Export job appears in Export Manager app.
```

---

## Search & Filter Details

### Search tabs (app top level)
- **Providers**, **Packages**, **Titles**

### Filters — Packages
- **Selection status**: All / Selected / Not Selected
- **Content type**: Aggregated Full Text, E-Book, E-Journal, Mixed Content, etc.
- **Tags** (hidden when user lacks tag permissions — UIEH-1313)
- **Sort**: Relevance (default), Package name

### Filters — Titles
- **Selection status**: All / Selected / Not Selected
- **Publication type**: Book, Journal, Database, etc.
- **Packages** facet — added in Ramsons (UIEH-1350/9.0.0); opens "Filter packages" modal
- **Tags** (hidden when user lacks tag permissions)
- **Sort**: Relevance (default), Title name

### Filters — Providers
- **Tags**
- **Sort**: Relevance (default), Provider name

### Package detail record — Titles accordion (in-list search)
- Search within field with reset ("x") button (UIEH-1466)
- Filter by Publication type (single-select since Umbrellaleaf, UIEH-1463)
- Filter by Access status type
- Filter by Tags
- Sort options
- Column chooser (Actions button menu, added Umbrellaleaf UIEH-1455)
- Columns: Title name, Tags (added Trillium UIEH-1449), Managed coverage, Custom coverage (Trillium UIEH-1442)
- Hidden title visibility shown in status column (Umbrellaleaf UIEH-1464)

**Full "Actions" menu contents on the Titles accordion (confirmed, C1259792)** — the Actions button opens all of the following in a single dropdown (don't split these into separate menus in expected results):
- `Search by tags only` checkbox
- `Search by access status types only` checkbox
- `Sort options`: `Relevance`, `Title`
- `Selection status`: `All`, `Selected`, `Not selected`
- `Publication type` single-select dropdown — full option list: `All` (default), `Audiobook`, `Book`, `Book Series`, `Database`, `Journal`, `Newsletter`, `Newspaper`, `Proceedings`, `Report`, `Streaming Audio`, `Streaming Video`, `Thesis & Dissertation`, `Website`, `Unspecified`
- `Show columns` checkboxes (all checked by default): `Status`, `Managed coverage`, `Custom coverage`, `Managed embargo period`, `Custom embargo period`, `Publication type`, `Access status type`, `Tags`

**Provider record's own Packages accordion mirrors this pattern (C1375907–C1375917, C1385306–C1385308):** same search/filter/sort/column-chooser controls, with column selections and applied filters persisting across navigating to a Package record and back, and across editing the Provider record. `Package name` is the one column that can never be hidden via the column chooser.

---

## Settings → eHoldings

| Setting Page | Description |
|---|---|
| Knowledge Base credentials | EBSCO API key + customer ID (required for app to function) |
| Root proxy | Default proxy for all resources; changing it overrides all inherited proxies |
| Custom labels | Up to 4 display labels for institution-specific title metadata |
| Usage consolidation | SUSHI credentials, currency, fiscal year for cost-per-use |
| Access status types | Custom labels for title access status; shown as filter option |
| Assigned users | Users assigned to each KB credential |

HTML page title format for Settings pages: `<<App name>> settings - <<selected page name>> - FOLIO` [confirmed, UIEH-1387]

---

## Export Feature

- **Package export**: Actions → `Export package (CSV)` — opens `Export settings` modal
- **Resource export**: Actions → `Export title package (CSV)` — opens `Export settings` modal
- **Export limit**: Maximum 10,000 titles per package export (UIEH-1336); "Export" button disabled when limit exceeded
- **Export fields (package)**: Package Name, Provider Name, Package Type, Content Type, Holdings status, Custom Coverage, Show To Patrons, Proxy, Access Status Type, Tags, Notes, Package/Provider Level Token
- **Export fields (title)**: Title name, Holding status, Coverage statement, Custom/Managed coverage dates, Proxied URL (Sunflower+), Custom labels, Access status type, Tags, Notes
- **Export processing**: Asynchronous via Export Manager; toast confirms job start with Job ID
- **UC Export**: `Export titles` action under Usage & analysis accordion (up to 200,000 titles)

---

## Agreements Integration

From any package or title+package record:
- **`New`** button in Agreements accordion — opens new agreement in a new tab (Sunflower+ UIEH-1446)
- **`Add`** button — links an existing agreement
- **`Delete agreement line`** — unlinks agreement; confirmation modal shown

Linked agreements appear in both eHoldings record and the Agreements record (bidirectional). ERM interface is optional as of Umbrellaleaf (UIEH-1476).

---

## Usage & Analysis (Cost-Per-Use)

Requires:
1. `Data - UI-Eholdings Costperuse - View` capability
2. Usage Consolidation configured in Settings (SUSHI credentials + Usage Consolidation ID + client ID)

View options per package/resource:
- **Summary table** — cost, usage, % of usage, coverage
- **List of titles** — per-title breakdown with cost, usage columns
- **Export titles** — up to 200,000 titles; toast on start; CSV downloaded from Export Manager

Locale-based number/date formatting applied to exported files (Umbrellaleaf).

---

## ECS / Multi-Tenant Notes

No ECS-specific test cases found in this section (0 cases with `custom_ecs_enabled = true`). eHoldings is a NonECS app — all tests run in single-tenant context.

---

## Recent Changes by Release

### Umbrellaleaf (R2 2026) — 11.1.x / 11.2.x
- Loading indicator shown under Package Titles list when titles are being updated (UIEH-1471)
- Publication type filter in Titles accordion: changed to single-select radio button (UIEH-1463)
- Package-Title (Resource) record: "Hidden" HLM visibility shown in status column (UIEH-1464)
- Column chooser added to "Search within" Actions button menu in Package Titles accordion (UIEH-1455)
- Fix: "Undefined" in Packages facet input when facet selection canceled (UIEH-1470)
- Fix: "Publication type" column empty in Titles accordion (UIEH-1487)
- Tags label updated in Titles accordion of Package record (UIEH-1488)
- UC-exported titles use locale-based formatting (UIEH from changelog)
- Fix: Access Status Types show createdDate when updatedDate not available (UIEH-1490)

### Trillium (R1 2026) — 11.0.x
- Package Title List: MCL component applied; managed + custom coverage dates shown in columns (UIEH-1442)
- Package Title List: Tags column added with column width adjustments (UIEH-1449)
- Package Record Title List Accordion: improved visibility of Search Within / Filter / Sort (UIEH-1440)
- Selected search option in Titles accordion now retained after collapse/expand (UIEH from 11.0.1)
- Agreements accordion: "New" button opens new Agreement in a new tab (UIEH-1446)

### Sunflower (R1 2025) — 10.0.x
- Resource (Title+Package) detail record: `Proxied URL` field now displayed (UIEH-1419)
- Agreements accordion: revised "Are you sure you want to unassign agreement?" message (UIEH-1420)
- Agreements accordion: tooltips added to New and Add buttons (UIEH-1424)

### Ramsons (R2 2024) — 9.0.x / 9.1.x
- Titles tab: `Packages` facet added to search (UIEH-1350)
- Title detail record: Packages accordion honors Packages facet selection (UIEH-1351)
- Settings: PaneCloseLink added to Assigned users pane (UIEH-1372)
- Custom labels: dashes removed below labels display (UIEH-1361)
- Settings HTML page title format standardized (UIEH-1387)
- Filter by Tag hidden when user lacks related permissions (UIEH-1313)

---

## Key Business Rules for Test Cases

> Every rule tagged with source: (cases) = confirmed by ≥3 case titles/steps; (github) = from source code/translations; (jira) = from story/bug descriptions.

1. **EBSCO is the only supported external KB** — without valid KB credentials in Settings, eHoldings shows no data (cases + github)
2. **Changes are real-time** — selecting/deselecting a package in FOLIO immediately reflects in EBSCO Holdings Management; proxy/token updates may take a few minutes (cases + github toast)
3. **Cannot delete KB packages or titles** — only custom packages/titles created locally in FOLIO can be deleted; KB content is read-only for deletion (cases + jira)
4. **Select/unselect requires a dedicated capability** — `Data - UI-Eholdings Records - Edit` alone does NOT allow adding/removing holdings; the `Procedural - UI-Eholdings Package-Title Select-Unselect - Execute` capability is also required (cases x8)
5. **Custom packages require Name + Content type** — these are the only required fields at creation (cases + github)
6. **Custom titles must be linked to a package** — field `Package *` is required; standalone titles cannot exist (github - `title.package.isRequired`)
7. **Proxy hierarchy: Resource > Package > Provider** — the most specific proxy setting wins; changing root proxy overrides all inherited proxies (cases + github warning message)
8. **Coverage set at package and resource levels** — resource-level coverage overrides package-level for that specific title; multiple date ranges allowed (cases x12+)
9. **Custom labels: up to 4, configured in Settings** — values are per Resource, labels apply across all title records institution-wide (cases + github)
10. **Usage & analysis requires two conditions** — the `Data - UI-Eholdings Costperuse - View` capability AND Usage Consolidation configured in Settings with valid SUSHI credentials (cases x5)
11. **Agreement linking is bidirectional** — linking a package/title in eHoldings creates an Agreement Line in the Agreements app; visible in both places (cases + github)
12. **Tags can be removed from records but not deleted from the system** — removing a tag only unlinks it; the tag remains available for selection (cases)
13. **Filter by Tag is hidden when user lacks tag permissions** — not shown rather than shown disabled; applies to every Tags-related control consistently: the `Search by tags only` checkbox AND the `Tags` checkbox in `Show columns` are both fully absent (not disabled) — confirmed on both the Titles-accordion Actions menu and the Provider record's own Packages accordion; every other filter/sort/column control remains usable for the restricted user (cases + UIEH-1313, C1385307)
14. **Search defaults to Relevance sort** — alphabetical sort must be explicitly selected (cases)
15. **Selection status filter defaults to "All"** — both selected and unselected are shown by default (cases)
16. **Multiple packages per title** — the same title can appear in multiple packages; each is a separate Resource with independent selection/configuration (cases)
17. **Provider records not editable beyond proxy/tags/notes** — names, package lists, title data come from EBSCO KB and cannot be changed in FOLIO (cases + github)
18. **`Export package (CSV)` / `Export title package (CSV)` were DEPRECATED and removed entirely** (per UIEH-1324/UIEXPMGR-57, confirmed C359166) — neither Actions menu item exists anymore on Package or Title detail views, and Export Manager's own `Job type` facet no longer has an `eHoldings` checkbox at all. Don't write a case expecting these actions to exist; this correction supersedes any older documentation (including this file's own prior wording) that described a working async Package/Title CSV export via Export Manager — that feature is gone, not just permission-gated.
19. **"Export" button disabled when no fields selected** — user must select at least one field in the Export settings modal (cases x2)
20. **A user already assigned to one KB cannot be assigned to another KB** — modal "Already assigned to a knowledge base" is shown (cases x1)
21. **Assigned users are sorted by Last name alphabetically** — in the Assigned Users list at Settings (cases)
22. **Deleting an Access status type removes it from all assigned records** — confirmation modal shows count of affected records; also confirmed the deleted type disappears from the Packages tab's own `Access status types` filter/facet accordion, not just from individual records (jira UIEH story + C590793)
22a. **Settings access is independent of main-app access** — a user whose eHoldings app view fails with the "missing permissions" access error can still open `Settings > eHoldings` directly and use every settings sub-page (`Root proxy`, `Custom labels`, `Access status types`, `Assigned users`, `Usage consolidation`) as long as they hold the corresponding Settings-level permissions; don't assume a blocked main app means Settings is blocked too. (cases — C357013)
23. **Hidden title visibility indicated in Titles list (Umbrellaleaf+)** — "Hidden" shown in status column of Package title list MCL (github UIEH-1464)
24. **Packages facet on Titles search opens a "Filter packages" modal** (Ramsons+) — selection persists as facet filter; cancel clears facet (cases + UIEH-1350)
25. **Agreements accordion "New" button opens new Agreement in new tab** (Trillium+) — prevents losing eHoldings navigation context (jira UIEH-1446)
26. **Custom package/title duplicate-name creation shows a clean, user-facing toast — never the raw backend error** — `Custom Package with the provided name already exists` / `Custom Title with the provided name already exists`; the toast must NOT include the raw "400 Bad Request on POST request for ..." text or a "1009:" error-code prefix. The name uniqueness check is **case-insensitive** (entering the existing name in a different case still triggers the toast). On this toast, the New pane stays open/unsaved and no duplicate record is created. (cases — C1404904, C1404905)
27. **Custom Embargo period field is conditionally rendered, like Coverage** — a Resource without any Custom embargo period set does not show the "Custom embargo period" field at all in the detail view; it only appears once a value has been added via Actions → Edit. Adding/editing/removing Custom embargo is independent of whether a Managed embargo period exists on the same Resource — test the with/without-Managed-embargo variants separately since the expected end-state differs only in whether the Managed field also shows. (cases — C421984, C421988)
28. **`proxiedUrl` is a real field on the `/eholdings/resources/{id}` API response** (Sunflower+, UIEH-1419) — present with a value whenever the Resource's effective Proxy is not "None"; verify via Network tab response body, not just UI display, since this is the field backing the "Proxied URL" UI label. (cases — C387517)
29. **"Packages" facet on Titles search only appears after a search returns results** — it is not shown on the empty/pre-search state, and it does not exist at all on the Providers or Packages toggles (Titles-search-only). Facet selections and the search query persist when opening a Title record from the result list and closing it again (`x` icon) — user returns to the exact same filtered/faceted list, not a reset one. Changing the facet selection while a Title record is open updates the facet's dropdown options to only the packages returned by the underlying title search. (cases — C386520)
30. **Usage Consolidation's "Start month for usage statistics" setting controls the 12-month window shown in every Usage & analysis table** — regardless of which `Platform` option (`All platforms` / `Publisher platforms only` / `Non-publisher platforms only`) or `Year` is subsequently selected, the table's 12 month-columns always start from the configured start month, not from January. Re-verify the start month after switching Platform, not just after the initial View click. (cases — C357547)
31. **Usage & Analysis CSV export column names are literally `Cost` and `Cost_per_use`** — these are the two columns whose number formatting follows the tenant's locale (set via `PUT /locale`), confirmed with a non-English locale (e.g. `fa-IR`) producing visibly different digit formatting than the English-locale export of the same data. (cases — C1030063)

---

## Authoring style (measured 2026-07-23)

eHoldings leans **`Functional` (~62%)** with ~35% `Other`, median ~6 steps, `User Journey` ~9%. Cases are compact and behavior-focused: most verify one accordion/filter/search/toast behavior on a Provider/Package/Title record (e.g. "filter retained after editing the Provider", "duplicate custom-package name toast"). Preconditions carry the KB configuration (KB credentials in Settings, a Selected package/title, tags/access-status-types set up) and any Usage-Consolidation setup for cost-per-use cases. Use `Functional` for record/search/filter behavior; `Other` for pure display/label checks. Because the Provider record's own Packages accordion mirrors the Titles accordion, filter/column/sort behaviors are tested on **both** surfaces — don't assume one covers the other.

---

## Known Gaps / Items to Verify

> Strings or rules that could not be confirmed from two independent sources. Verify in env before relying on them in test cases.

- [ ] Toast for lag warning `"Updates to title selection status in the Titles accordion may take several seconds to display."` — found in Jira story (open) only; not yet in test cases
- [ ] Exact text of the "Are you sure you want to unassign agreement?" revised message (Sunflower UIEH-1420) — not captured in test cases
- [ ] Exact confirmation modal body when selecting an entire package (`Add all titles in a package to holdings`) — from source only
- [x] ~~"Proxied URL" field label exact text on Resource detail record (Sunflower+)~~ — resolved: API field is `proxiedUrl` on `/eholdings/resources/{id}` response, confirmed present whenever Proxy ≠ "None" (see Rule 28, C387517)
- [ ] Column chooser behavior in Titles accordion Actions menu (Umbrellaleaf UIEH-1455) — in Jira/changelog but no test case covering it yet
- [ ] Agreements accordion tooltip text for New and Add buttons (Sunflower UIEH-1424) — verify in env

> Self-assessment enrichment round (2026-07-22, per report priority #4): read C359166, C357013, C590793 to close the 3 misses the self-assessment flagged for this file. Most valuable finding: Rule 18's description of a working Package/Title CSV export was **stale** — that export was deprecated (UIEH-1324) and this file has been corrected rather than just appended to, since the old wording would have led to writing a test for a feature that no longer exists. Also added the Settings-access-independent-of-app-access rule (22a) and a refinement to the Access-status-type deletion rule (22) re: the Packages-tab filter accordion.
