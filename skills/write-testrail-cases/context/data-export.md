# FOLIO Data Export App — Business Logic Context

> Generated by the `build-app-context` skill on 2026-06-16 (overwrites previous version sourced from FOLIO docs).
> Sources: TestRail section 1124 (27 direct cases, releases Ramsons / Sunflower / Trillium; plus child section "Data Export: Consortia" id 27637 with 56 ECS cases, mostly unread — see ECS section below; and child section "Data Export - Obsolete" id 21206, excluded)
>          GitHub: folio-org/ui-data-export (README, CHANGELOG, en.json, en_US.json)
>          Jira: project UIDEXP (stories + bugs)
> Strings marked [confirmed] appear in both GitHub translations and TestRail cases.
> Strings marked [from source] come from GitHub only — verify in env before asserting.
> Strings marked [from cases] come from TestRail cases.

---

## What is Data Export

The Data Export app exports bibliographic, holdings, item, and authority data from FOLIO as MARC 21 (.mrc) files. Staff upload a .csv file of UUIDs or a .cql file containing a CQL query, select a job profile, confirm the record type, and the system generates a downloadable MARC file logged in the Logs pane. Exports can also be triggered from the Inventory and MARC Authority apps via the Actions menu (quick export), where the UUID .csv downloads immediately while the .mrc file is generated asynchronously and logged in Data Export.

---

## Key Terms

| Term | Definition |
|---|---|
| Job profile | Top-level export configuration that links a record type to a field mapping profile. Entry point for running an export. |
| Field mapping profile | Defines which FOLIO fields map to which MARC fields in the output, including transformations and field suppression. |
| Transformation | A mapping rule inside a field mapping profile that routes an Inventory/Holdings/Item field to a specific MARC field, indicators, and subfield. |
| Fields suppression | A setting in a field mapping profile that excludes specified MARC tags (three-digit codes, comma-separated) from the generated export output. |
| SRS | Source Record Storage — the FOLIO store holding the original MARC records. Instances with SRS records export their full underlying MARC; FOLIO-native instances get simplified generated MARC. |
| Quick export | Export triggered from Inventory (Actions → Export instances (MARC)) or MARC Authority (Actions → Export selected records (CSV/MARC)). UUID .csv is downloaded immediately; .mrc appears asynchronously in Data Export logs. |
| Running accordion | Accordion in the Jobs pane that shows in-progress export jobs with a progress bar. |
| View all | Button on the Logs pane that opens the full log history with search and filter capabilities (replaces the 25-record default view). |
| Purge window | Authority records set for deletion are accessible for 7 days; after that they are purged from SRS and cannot be exported by any profile. |

---

## Record Hierarchy / Architecture

```
Job Profile  (entry point — what to run)
  └── Field Mapping Profile  (what fields to include and how to transform)
        ├── FOLIO Record Types (checkbox selection — at least one required)
        │     ├── Source Record Storage (entire record)   [exports full SRS MARC]
        │     ├── Inventory instance (selected fields)    [generated on-the-fly]
        │     ├── Holdings                                [appended to MARC output]
        │     └── Item                                    [appended to MARC output]
        ├── Transformations  (MARC field/indicator/subfield mappings)
        └── Fields Suppression  (MARC tags to exclude; comma-separated 3-digit codes)
```

---

## Lifecycle / Workflow

### Export Job Lifecycle

| Phase | UI representation |
|---|---|
| File uploaded | Progress bar appears under "Running" accordion in Jobs pane |
| Processing | "Select job profile to run the export" pane displayed; job profile selection & record type confirmation |
| Running | Progress bar shows in "Running" accordion; "Triggered by {userName}" and "Job profile: {jobProfile}" shown |
| Completed | Row added to Logs table with Status = "Completed"; filename is a hyperlink |
| Completed with errors | Row added with Status = "Completed with errors"; filename is a hyperlink; click row to see per-record errors |
| Fail | Row added with Status = "Fail"; filename is NOT a hyperlink (non-clickable); click row to see error message |

### Export Job Statuses

| Status | Meaning |
|---|---|
| **Completed** | All records exported successfully; .mrc file downloadable |
| **Completed with errors** | Partial export; some records failed; .mrc file still downloadable; per-record error log available |
| **Fail** | No records exported; no .mrc file; click the row (not the filename) to see errors |

### Profile Lifecycle

| Action | Condition |
|---|---|
| Job profile — can edit/delete | Profile has never been used in an export |
| Job profile — cannot edit or delete | Profile has been used in at least one export; Edit and Delete actions are disabled |
| Job profile — can duplicate always | Duplication is always allowed regardless of use |
| Field mapping profile — can edit/delete | Not currently assigned to any job profile |
| Field mapping profile — cannot edit or delete | Assigned to at least one job profile |

---

## Main Sections / UI Structure

### Panes

- **Jobs** pane (left) — "Drag and drop" area with "or choose file" button; "Running" expanded accordion [confirmed]
- **Logs** pane (main) — table of 25 most recent export jobs; "View all" button [confirmed]
- **Search & filter** pane (left, View all only) — ID search, Errors in export, Started running, Ended running, Job profile, User accordions [confirmed]
- **Data export** pane (Settings) — lists "Job profiles" and "Field mapping profiles" [confirmed]

### Accordions

| Accordion | Location | Default state |
|---|---|---|
| Running | Jobs pane | Expanded |
| Errors in export | Search & filter (View all) | Expanded |
| Started running | Search & filter (View all) | Collapsed |
| Ended running | Search & filter (View all) | Collapsed |
| Job profile | Search & filter (View all) | Collapsed |
| User | Search & filter (View all) | Collapsed |

### Logs Table Columns (default and View all)

`File name` · `Status` · `Total` · `Exported` · `Failed` · `Job profile` · `Started running` · `Ended running` · `Run by` · `ID`

- Default sort: **Ended running** descending (most recent on top)
- All columns are sortable; all show "Up-down" icon except "Ended running" which shows "v" (active sort)
- Sorting is preserved across page reload (F5) and navigation away and back to the app
- Sorting is reset to default "Ended running" descending when navigating from "View all" back to the Data export landing page via the header button

### Key Navigation Paths

- `Data export` (header icon) — opens landing page with Jobs + Logs panes
- `Settings > Data export > Job profiles`
- `Settings > Data export > Field mapping profiles`
- `Inventory > [instance list] > Actions > Export instances (MARC)` — bulk quick export (multiple selected)
- `Inventory > [instance detail] > Actions > Export instance (MARC)` — single quick export
- `Inventory > [instance list] > Actions > Save instances UUIDs` — download UUID .csv for manual upload
- `Inventory > [instance list] > Actions > Save Instances CQL query` — download .cql file for manual upload
- `MARC authority > [authority list] > Actions > Export selected records (CSV/MARC)` — quick export
- `MARC authority > [authority list] > Actions > Save authorities CQL query` — download .cql file

---

## Exact UI Texts

> Use these verbatim in expected results. Never paraphrase.

### Toast Messages (Callouts)

| Event | Toast text | Source |
|---|---|---|
| Field mapping profile created | `The field mapping profile **{name}** has been successfully **created**` | [confirmed] |
| Field mapping profile saved (edited) | `The field mapping profile **{name}** has been successfully **saved**` | [confirmed] |
| Field mapping profile deleted | `Mapping profile **{name}** has been successfully **deleted**` | [confirmed] |
| Job profile created | `Job profile **{name}** has been successfully **created**` | [confirmed] |
| Job profile edited | `Job profile **{name}** has been successfully **edited**` | [confirmed] |
| Job profile deleted | `Job profile **{name}** has been successfully **deleted**` | [confirmed] |
| Job profile duplicated | `Job profile **{name}** has been successfully **created**` | [confirmed] |
| Transformations added | `**{count}** **{transformation(s) have}** been successfully **added**` | [confirmed] |
| Transformations updated | `The transformations have been updated` | [confirmed] |
| Quick export triggered (Inventory / MARC Authority) | `The export is complete. The downloaded .csv contains selected record's UUID. To retrieve the .mrc file, please go to the Data export app.` | [from cases] |
| Server error | `Server communication problem. Please try again` | [confirmed] |
| File too large | `The uploaded file exceeds the allowed file size (500Mb).` | [confirmed] |
| Nothing to export | `Nothing to export: no .mrc file generated.` / `No exported records: nothing to export` | [confirmed] |

> ✅ Entire "Exact UI Texts" for Data export re-verified against `ui-data-export/en_US.json` (2026-07-21) — all rows match.

**Invalid file modal:** title `Invalid file`; message `Only file with .csv or .cql extension can be uploaded`; button `Choose other file to upload`. **Delete confirmation modals:** `Delete job profile` / `The job profile <b>{name}</b> will be deleted.`; `Delete mapping profile` / `The mapping profile <b>{name}</b> will be deleted.` (or `Cannot delete mapping profile` / `This mapping profile cannot be deleted, as it is used in the following job profiles: <b>{jobProfilesNames}</b>.`). [confirmed]

### Modal / Dialog Titles

| Modal | Trigger | Source |
|---|---|---|
| `Select job profile to run the export` | After uploading .csv or .cql file to Jobs pane | [confirmed] |
| `Are you sure you want to run this job?` | After clicking a job profile in the select modal | [confirmed] |
| `Invalid file` | Uploading a file with extension other than .csv or .cql | [confirmed] |
| `Delete job profile` | Delete action on a job profile | [confirmed] |
| `Delete mapping profile` | Delete action on a field mapping profile | [confirmed] |
| `Cannot delete mapping profile` | Attempting to delete a mapping profile assigned to a job profile | [confirmed] |

### Modal Body Texts

| Modal | Body text | Source |
|---|---|---|
| Invalid file | `Only file with .csv or .cql extension can be uploaded` | [confirmed] |
| Invalid file — action button | `Choose other file to upload` | [confirmed] |
| Cannot delete mapping profile | `This mapping profile cannot be deleted, as it is used in the following job profiles: **{jobProfilesNames}**.` | [confirmed] |
| Are you sure (run job) | `You have selected **{profile}** to run the export.` | [confirmed] |
| Are you sure — record type dropdown label | `UUIDs provided for this export` | [confirmed] |

### Button Labels (key actions)

`Run` · `View all` · `or choose file` · `Reset all` · `Apply` · `Search` · `Actions` · `Save instances UUIDs` · `Save holdings UUIDs` (Inventory Holdings tab → Actions; seeds a holdings export) · `Next >` · `Save & close` · `Save & Close` · `New` · `Assign/Unassign` · `Select application`

Empty logs state: `The list contains no items` (shown in the Logs pane when no export jobs exist). [confirmed — C350511]

### Error / Warning Messages (per-record log errors)

| Condition | Message text | Source |
|---|---|---|
| Invalid CQL syntax | `ERROR Invalid CQL syntax in "{filename}"` | [from cases] |
| Instance with duplicate SRS records | `ERROR Instance with HRID: {hrid} has following SRS records associated: {uuid1}, {uuid2},...` | [from cases] |
| Record not found | `ERROR Record not found: {uuid}` | [from cases] |
| Authority set for deletion (wrong profile) | `Authority record {UUID} is set for deletion and cannot be exported using this profile` | [from source, verify in env] |
| Non-deleted authority (deleted profile) | `This profile can only be used to export authority records set for deletion` | [from source, verify in env] |
| Repeated UUID in submitted .csv file | `ERROR UUID {uuid} repeated {n} times.` (e.g. `UUID 04e66795-0286-4520-906c-bae76f78999e repeated 2 times.`) — status shows "Completed with errors"; "Total" counts every row incl. duplicates, "Exported" counts only distinct valid UUIDs, "Failed" counts the repeated occurrences | [cases — C476789] |
| `/data-export/export-deleted` with a future/empty date range | `ERROR Error while reading from input file with uuids or file is empty` — Status = "Fail", filename NOT a hyperlink, "Total"/"Exported" both show 0 | [cases — C566502] |

### Validation Error Messages

| Condition | Message | Source |
|---|---|---|
| Date filter — no end date | `Please enter an end date` (red text below "To" field; "To" field highlighted red) | [from cases] |
| Date filter — no start date | `Please enter a start date` (red text below "From" field; "From" field highlighted red) | [from cases] |
| Date filter — start > end | `Start date is greater than end date` (red text below "To" field) | [from cases] |
| Transformation row invalid | `Please check your input` | [confirmed] |
| Fields suppression invalid format | `Suppressed fields can be represented by three digits only and need to be separated by a comma.` | [from source] |
| Record type mismatch in transformations | `Selected record types do not match specified transformations` | [from source] |

---

## Status Values

### Export Job Status
`Completed`, `Completed with errors`, `Fail`

*(Note: "In progress" appears in the Running accordion during processing, not in the Logs table.)*

---

## Capability Sets (Eureka)

> Confirmed names from TestRail case [877101] (Trillium). Older lowercase variants (`data - UI-Data-Export - edit`) appear in pre-Trillium cases — use PascalCase versions in new test cases.

| Action | Capability Set |
|---|---|
| View export logs only | `Data - UI-Data-Export - View` |
| Upload files, export, download, view logs | `Data - UI-Data-Export - Edit` |
| View Data Export settings only | `Settings - UI-Data-Export Settings - View` |
| View and edit Data Export settings (profiles) | `Settings - UI-Data-Export Settings - Edit` |

> ⚠️ Older legacy permission names found in Ramsons/Sunflower cases — do not use in new test cases:
> - `data - UI-Data-Export - edit` (lowercase)
> - `data - UI-Data-Export - view` (lowercase)
> - `settings - UI-Data-Export Settings - view` (lowercase)
> - `UI-Data-Export - data/edit` (slash-notation)
> - `Data export: Can upload files, export, download files and view logs` (legacy display name)
> - `Settings (Data export): Can view only` (legacy display name)

---

## Common Verification Patterns

> Copy these into expected results. They reflect how the team actually writes assertions for this area.

### Pattern 1: Successful export with Edit capability

```
Action:   Navigate to Data export → click "or choose file" in Jobs pane → upload .csv file
          → "Select job profile to run the export" pane opens → click job profile
          → "Are you sure you want to run this job?" dialog → select record type from
          "UUIDs provided for this export" dropdown → click "Run"
Expected: Progress bar appears under "Running" accordion
          After job completes: new row in Logs table with:
            Status = "Completed"
            Job profile = "<selected job profile name>"
            Total = Exported (same value, no failures)
            File name is a hyperlink
          Clicking the filename hyperlink downloads the .mrc file
```

### Pattern 2: Export with duplicate SRS records (Completed with errors)

```
Action:   Run export for instances where at least one instance has multiple SRS records
Expected: Status = "Completed with errors"
          Failed column shows count: "{n} duplicate(s)"
          Clicking the row (not the filename) opens error log:
            ERROR Instance with HRID: {hrid} has following SRS records associated: {uuid1}, {uuid2},...
          Each affected instance has a separate error entry with UUID, HRID, Title, and link to record
          File name IS a hyperlink (partial export was generated)
```

### Pattern 3: View-only capability — export blocked

```
Action:   Log in with only Data - UI-Data-Export - View capability set
          → Navigate to Data export app
Expected: "Drag and drop" area in Jobs pane is disabled (cannot upload files)
          "Logs" pane is displayed with the table and "View all" button (enabled)
          Settings app is NOT available in the app menu
          Only "Data export" app is available
```

### Pattern 4: Quick export from Inventory (multi-record)

```
Action:   Inventory → select instance checkboxes → Actions → "Export instances (MARC)"
Expected: .csv file downloads immediately to local machine
          File name format: QuickInstanceExport_{YYYY-MM-DD}T{hh_mm_ss}+{hh_mm}.csv
          .csv contains UUIDs of selected instances without duplicates
          In Data export app: new row added to Logs with:
            Status = "Completed"
            Job profile = "Default instances export job profile"
            Run by = current user's username
          Clicking filename hyperlink downloads .mrc file
```

### Pattern 5: Sorting persistence across reload and navigation

```
Action:   On "View all" page, click a column header to sort (e.g., "ID" descending)
          → Reload page (F5)
Expected: Sorting preserved; "v" icon displayed next to "ID" column
          Records remain sorted by ID descending after reload

Action:   Click "Data export" header button while on "View all" page
Expected: Returns to landing page; default sort restored:
          "Ended running" descending; "v" icon next to "Ended running"
```

---

## File Naming Conventions

| Scenario | File name format |
|---|---|
| Quick export .csv (Inventory or MARC Authority) | `QuickInstanceExport_{YYYY-MM-DD}T{hh_mm_ss}+{hh_mm}.csv` |
| Export .mrc (single file, ≤100,000 records) | `{jobId}.mrc` |
| Export .zip (>100,000 records — multiple .mrc files) | `{jobId}.zip` containing `{jobId}-1.mrc`, `{jobId}-2.mrc`, ..., `{jobId}-n.mrc` |

---

## Default Job Profiles (System-Provided)

| Profile name | Record type | Notes |
|---|---|---|
| Default instances export job profile | Instance | Uses SRS MARC for MARC instances; generates simplified MARC for FOLIO-native instances |
| Default authority export job profile | Authority (non-deleted) | Rejects records set for deletion with per-record error |
| Deleted authority export job profile | Authority (set for deletion) | Rejects non-deleted records; only exports within 7-day purge window |
| Default holdings export job profile | Holdings | Exports holdings data |

### Default Field Mapping Profiles

| Profile name |
|---|
| Default authority mapping profile |
| Default holdings mapping profile |
| Default instances mapping profile |

---

## Holdings → MFHD Field Mapping (Default holdings export job profile)

> Generated on-the-fly (no underlying SRS for Holdings). Confirmed field-by-field mapping — use verbatim in expected results when asserting exported MFHD content. (cases — C350511)

| FOLIO Holdings field | MARC (MFHD) target |
|---|---|
| Holdings HRID | 001 |
| Associated Instance HRID | 004 |
| Permanent location | 852 `$b` |
| Holdings statement + notes | 866 `$a` `$z` |
| Holdings statement for supplements + notes | 867 `$a` `$z` |
| Holdings statement for indexes + notes | 868 `$a` `$z` |
| Holdings UUID | 999 ff `$i` |

- Applies regardless of the Holdings record's underlying source (MARC-created-via-data-import or FOLIO-native) — both are exported through the same on-the-fly MFHD generation, only the Instance side of the record varies by source. (cases — C350518)
- Trigger: submit a .csv of Holdings UUIDs (via Inventory Holdings tab → Actions → "Save Holdings UUIDs") to Data export → run "Default holdings export job profile" → select record type "Holdings" on the confirmation modal.

## API-Only Export Endpoints (no dedicated UI trigger)

These are exercised via Postman/direct API call; the resulting job still appears in the Data Export Logs pane like any UI-triggered export.

| Endpoint | Purpose | Key behaviors |
|---|---|---|
| `POST /data-export/export-deleted` `{ "from": "YYYY-MM-DD", "to": "YYYY-MM-DD" }` | Export deleted MARC bib (Instance) records within a date range (UTC) | Filename: `deleted-marc-bib-records-<data-export-job-id>` (`.mrc` if one file, `.zip` if more); exported records show **LDR 05 = `d`**; 005 field falls within the requested date range; omitting both dates or requesting a future-only range with no matching deletions → Status "Fail" with `ERROR Error while reading from input file with uuids or file is empty`, 0/0 in Total/Exported, filename not a hyperlink. (cases — C566502) |
| `GET /data-export/download-record/{recordId}?idType=INSTANCE` | Direct single-record MARC download by Instance UUID (backs the Inventory quick-export flow) | Default response includes the full 999 field incl. **999 ff** subfields; add `&suppress999ff=true` to omit the 999 **ff**-indicator field from the response while non-ff-indicator 999 fields still appear. The record from this endpoint (without the flag) is byte-identical to the .mrc produced by Inventory → Actions → "Export instances (MARC)" quick export, confirming quick export uses this same endpoint internally. A record downloaded with `suppress999ff=true` can be re-imported via Data Import ("Default - Create instance and SRS MARC Bib") without error. (cases — C895650) |

**Slice-size configuration test technique** (`POST /data-export/configuration {"key":"slice_size","value":"{n}"}`, see Rule 15): tested with `value=1` (one record per .mrc file inside the .zip), `value=N` (exact UUID count in the source file — single .mrc, no .zip), and `value=N-1` (produces exactly two .mrc files inside the .zip: one with N-1 records, one with 1). Always reset `slice_size` back to `100000` (the default) as the final step after this kind of test. (cases — C431148)

---

## ECS / Multi-Tenant Notes

> ⚠️ **Correction (2026-07-23):** the previous claim here — "No ECS-specific test cases found... all 27 cases have `custom_ecs_enabled = false`" — was wrong. It only checked section 1124's own direct cases and missed its child section. TestRail section 1124 has a dedicated child section **"Data Export: Consortia" (id 27637, 56 cases)**, titled `Consortia | ...` / `ECS | ...`, covering exactly this. Only 1 of the 56 has been read so far — treat the rest as an open gap (see Known Gaps).

**Confirmed from C877107 ("ECS | Verify data export authorization roles"):**
- Data Export has **no system-defined authorization roles** in Consortium Manager's Authorization Roles page, in either the Central or a Member tenant (searching for "Data Export" / "Data Export Admin" returns none) — unlike some other apps which do ship system-defined roles.
- A Data-Export role must be created manually: `Consortium manager > Authorization roles > Actions > New`, assigning capability sets `Data - UI-Data-Export - View, Edit` and `Settings - UI-Data-Export Settings - View, Edit` (plus whatever Inventory/MARC Authority capabilities the workflow under test needs, e.g. `Data - UI-Inventory - Manage`, `Data - UI-Marc-Authorities Authority-Record - Edit`).
- Once assigned in the Central tenant and the user re-logs in, the Data export app (and any other apps whose capabilities were bundled into the role) becomes available — this is a **Central-tenant-only** effect; the case then re-checks the Member tenant's own Authorization Roles page and confirms it *also* has no system-defined Data Export role (i.e., the "no system role" fact holds independently per-tenant, it isn't just a Central-tenant gap).

**Titles suggesting further (unread) Consortia-section coverage, in order** (for future audit — not yet confirmed): exporting shared vs. local Instance records with Default/Custom job profiles, "local instances from Member 1 do not export for Member 2" (tenant isolation), Quick export of shared vs. local instances (with/without holdings) from a Member tenant, exporting with CQL query (incl. long ~10,000-record queries), large-volume export (~10,000 records), and share-then-export flows for MARC instances/authorities from a Member tenant.

---

## Key Business Rules for Test Cases

1. **Job profile used in export → cannot be edited or deleted** — Edit and Delete actions are disabled; the only option is to duplicate the profile. (cases + jira)
2. **Field mapping profile assigned to a job profile → cannot be edited or deleted** — `Cannot delete mapping profile` modal shown with list of linked job profiles. Must unassign (or delete) the job profiles first. (cases + github)
3. **Field mapping profile requires at least one FOLIO record type checkbox** — saving without any type selected returns a validation error. (github)
4. **Output format is always MARC** — no other output format option exists. (github)
5. **Default authority export profile rejects records set for deletion** — each such record produces a separate per-record error in the log; export may still show "Completed with errors" if some records were valid. (existing context)
6. **Deleted authority export profile rejects non-deleted records** — same logic in reverse; wrong profile always results in per-record errors. (existing context)
7. **Records past the 7-day purge window → "Record not found"** — even the correct deleted profile cannot export purged records; individual error per record. (existing context)
8. **UUID must match the correct record type at confirmation** — staff must select the right type from "UUIDs provided for this export" dropdown; wrong type causes systematic failures. (cases)
9. **Quick export: UUID .csv is immediate; .mrc is async** — .csv arrives in browser immediately; .mrc appears only in Data Export logs. (cases)
10. **Logs show 25 most recent by default; "View all" shows complete history** — logs are never deleted; all history retained permanently; search and filter available only in View all. (cases)
11. **Fail status = no .mrc file generated** — file name in the log row is not a hyperlink; clicking the row itself opens error details. (cases)
12. **Completed with errors = partial .mrc file exists** — file is downloadable; per-record error details available by clicking the row. (cases)
13. **Subfields in transformations must begin with `$`** — e.g. `$a`, `$b`; omitting `$` triggers the `Please check your input` validation message. (github)
14. **FOLIO-native instances generate simplified MARC** — less complete than SRS MARC; note in expected results when the instance source is FOLIO vs MARC. (existing context)
15. **Large exports (>100,000 records) generate a .zip archive** — the .zip contains numbered .mrc slices; default slice size is 100,000; configurable via POST /data-export/configuration `{ "key": "slice_size", "value": "100000" }`. (cases)
16. **Multiple export jobs run simultaneously (async)** — small jobs are not queued behind large jobs; all run in parallel. (cases)
17. **Token for export file download never expires** — files from past export jobs remain downloadable indefinitely. (cases)
18. **Only .csv or .cql file extensions are accepted** — any other extension triggers the "Invalid file" modal. (cases + github)
19. **Sorting is preserved across page reload (F5)** — including with active filters on the View all page. (cases)
20. **"Job profile" filter (View all) shows only profiles used in at least one export** — unused profiles do not appear in the dropdown; API: `job-profiles?used=true`. (cases)
21. **"Errors in export" filter: Yes includes "Completed with errors" AND "Fail"; No includes only "Completed"** (cases)
22. **"Run by" column sort order differs by view** — View all page: FirstName, LastName; landing page: LastName, FirstName. (cases)
23. **No system-defined "Data Export" or "Data Export Admin" authorization roles exist** — must be created manually by assigning capability sets. (cases)
24. **Numeric values in logs table use thousands separators** — e.g. `1,000` records; relevant for Total, Exported, Failed columns when counts exceed 999. (cases)
25. **Exporting a FOLIO Instance that is "Set for deletion" produces LDR position 05 = `d` in the exported MARC**, while an Instance that is merely staff-suppressed + suppressed-from-discovery (but not set for deletion) exports with a normal LDR05 (`n`/`c`, etc.). Don't conflate these two suppression-like states in export assertions — only "set for deletion" flips the Leader status code; discovery/staff suppression alone does not. (cases — C543846)
26. **The system Default mapping profile's Actions menu offers only `Duplicate`** — no `Edit` or `Delete` option at all (not merely disabled) for the built-in Default instance/holdings/authority mapping profiles; only a job-profile-linked *custom* mapping profile shows the "Cannot delete" gate described above. Test both cases separately: default profile (Duplicate-only) vs. custom-but-linked profile (blocked-delete-with-list). (cases — C15822, C15825)
27. **The "Failed" column's error+duplicate count is independent of a record's row position in the source file** — moving a failing/duplicate UUID to a different row in the CSV must produce the identical `{errors}, {duplicates} duplicate(s)` result as before the reorder; this was a past regression (`MDEXP-653`, "Failed column populated differently depending on place in file"). A Failed-column case with multiple error types should include a reordered-file variant to catch position-dependent bugs, not just one file layout. (cases — C422011)

---

## Authoring style (measured 2026-07-23)

Data Export: **`Other` ~93%**, median ~6 steps, `User Journey` ~0%. Cases run an export (upload UUID/CQL file → pick job profile → run) and verify the Logs row (status, counts, downloadable .mrc) or the exact per-record error, or a mapping/job-profile settings check. Mostly `Other`; keep compact. Preconditions carry the job/mapping profile and the input file. The ECS "Data Export: Consortia" subtree (56 cases) is largely unread — sample it before generating ECS export cases.

---

## Known Gaps / Items to Verify

- [ ] Toast text for quick export from MARC Authority (single record "Export (MARC)") — verified only for Inventory path; single-record MARC Authority path not in current test cases
- [ ] Empty .cql file vs invalid CQL — both show "ERROR Invalid CQL syntax" per cases 399097 and 397323; verify in env if distinction exists
- [ ] Maximum file upload size limit (500 MB) — from GitHub translation only; not explicitly tested in section 1124
- [ ] Transformation validation: exact behavior when Subfield starts with `$` but rest is blank — `Please check your input` flagged as [from source]
- [ ] Exact progress bar display text (`Began {time}`, `{countOfRecords} records`) — from en.json only; not explicitly asserted in test cases
- [ ] Authority export profile error messages exact wording — not in section 1124 test cases; sourced from existing context; verify in env
- [ ] `The transformations have been updated` toast — from GitHub only; no test case for editing existing transformations in section 1124
- [ ] **"Data Export: Consortia" section (id 27637, 56 cases) — 55 of 56 still unread.** Only C877107 (authorization roles) has been read; tenant-isolation behavior for shared/local instance export, Quick export from a Member tenant, and large-CQL-query export are all title-only guesses pending confirmation.

> Random spot-check (2026-07-23): picked one fresh uncited case at random — but from the *whole* section-1124 subtree rather than just the 27 direct cases, which surfaced that the file's own "ECS / Multi-Tenant Notes" claim ("no ECS-specific test cases") was stale/wrong — a 56-case ECS child section existed and had simply never been pulled. Corrected that section and flagged the remaining 55 cases as a known gap rather than guessing their content.

